<Window x:Class="FedMes.Desktop.Views.ForwardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Переслать сообщение"
        Width="430"
        Height="560"
        MinWidth="390"
        MinHeight="480"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource FedMesBackgroundBrush}">
    <Border Margin="16"
            Padding="18"
            Background="{DynamicResource FedMesSurfaceBrush}"
            BorderBrush="{DynamicResource FedMesDividerBrush}"
            BorderThickness="1"
            CornerRadius="18">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock FontSize="22"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource FedMesPrimaryTextBrush}"
                       Text="Переслать" />
            <TextBlock Grid.Row="1"
                       Margin="0,5,0,12"
                       Foreground="{DynamicResource FedMesSecondaryTextBrush}"
                       Text="Выберите чат, как в Android-версии" />

            <ListBox x:Name="ChatsListBox"
                     Grid.Row="2"
                     Margin="0,0,0,14"
                     Background="Transparent"
                     BorderThickness="0"
                     MouseDoubleClick="ChatsListBox_MouseDoubleClick">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="Margin" Value="0,2" />
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="ItemBorder"
                                            Padding="12,10"
                                            Background="{TemplateBinding Background}"
                                            CornerRadius="12">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="44" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <Border Width="38"
                                                    Height="38"
                                                    CornerRadius="19"
                                                    Background="{DynamicResource FedMesAccentBrush}">
                                                <TextBlock HorizontalAlignment="Center"
                                                           VerticalAlignment="Center"
                                                           Foreground="White"
                                                           FontWeight="SemiBold"
                                                           Text="{Binding AvatarText}" />
                                            </Border>
                                            <TextBlock Grid.Column="1"
                                                       Margin="10,0,0,0"
                                                       VerticalAlignment="Center"
                                                       FontSize="15"
                                                       Foreground="{DynamicResource FedMesPrimaryTextBrush}"
                                                       Text="{Binding Title}" />
                                        </Grid>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource FedMesHoverBrush}" />
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource FedMesSelectedBrush}" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>

            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Width="100"
                        Margin="0,0,10,0"
                        Click="CancelButton_Click"
                        Content="Отмена"
                        Style="{StaticResource SurfaceButtonStyle}" />
                <Button Width="120"
                        Click="ForwardButton_Click"
                        Content="Переслать"
                        Style="{StaticResource PrimaryButtonStyle}" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
